# Broker contract

Transport-neutral TypeScript types for `term-dock.broker/v1`. Both the desktop app and the future browser client import this package; keep it free of React, Tauri, browser APIs, and secrets.
