# Term Dock does not require an AI provider or an API key for its MVP.
# If you add a provider adapter, keep keys in the OS credential store; never
# commit them or place them in workspace definitions or deep links.
